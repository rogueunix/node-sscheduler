import * as moment from 'moment';
import { MomentInterval } from './types';
declare const _default: (n: number, startDate: moment.Moment, intervalMinutes: number, duration: number, reference: string | undefined) => MomentInterval[];
export default _default;
